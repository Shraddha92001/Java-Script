 for (let i = 0; i <= 10; i++) {
     console.log(`outer loop: ${i}`);
     for (let j = 0; j <= 5; j++) {
         console.log(`inner loop ${j} and outer loop ${i}`);
         //      for (let k = 0; k <= 10; k++) {
         //          console.log(`second inner loop ${k} and outer loop ${i} and inner loop is ${j}`);

         //      }
         //  
     }

 }