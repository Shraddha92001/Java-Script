//using Array in Function

const Array = [2000, 555, 100, 120]

function getArray(myArray) { //... is rest and spread  ---- put all element in array
    return myArray[3]
}
console.log(getArray(Array))
    //console.log(customer(2, 500, 6, 200, 400)) //access mnultiple all the value we use spread